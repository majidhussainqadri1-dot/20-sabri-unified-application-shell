# Repository Manifest — File 20

## Historical preserved source

- `SOURCE-ARCHIVE/20-sabri-unified-application-shell-1.0.0-FINAL.zip` is the immutable historical baseline.
- Root `CHECKSUMS.sha256` records that historical archive only.
- `SOURCE-PROVENANCE.md` preserves the import/provenance chain.

## Current source and release flow

- `sabri-unified-application-shell/` is the current reviewed plugin source.
- `.github/workflows/corrective-quality.yml` is the current **1.4.10 tenth-audit** release workflow.
- `STATUS.md` is lifecycle truth; current installable artifacts are generated from exact-head CI.

## Production-package constitution

For **1.4.10**, CI derives a clean production source set, excludes tests/development-only files, writes an embedded manifest, builds one canonical plugin root, rejects traversal/duplicate/wrong-root ZIP entries, proves source/stage/extracted file-set + SHA-256 parity, requires embedded/external manifest equality, and verifies archive SHA-256/CRC.

The exact-head gate additionally requires the tenth-pass ownership/health corrections: retired File20 local Home-feed runtime/state under File21 ownership; five executable File21 native Home/News slots with native-over-legacy single-render behavior; File25-only visual truth; reviewed File00 1.2.18 evidence without a production-safe implication; File26-only Search surface; provider health bound to actual advertised semantic versions; critical File20/File00 truth that cannot false-green Unknown/Unavailable/Incompatible; and the same critical gate for Emergency re-enable. All ninth-pass recovery/routing/uninstall protections and earlier PWA/privacy/release-ring/conditional boundaries remain required.

## Recovery/evidence boundary

Recovery snapshot format 2 distinguishes option absence from `null`; automatic rollback requires compatible snapshot format, File 20 code major and current settings schema. The selected target is revalidated under lock before a pre-rollback snapshot can affect retention. Current Emergency safety state and the monotonic settings-row counter are not rolled backward. Every restored option is verified after write. Activation snapshot integrity and the no-shared-front-page-restore boundary remain intact.

## Native-owner and lifecycle boundary

File21 owns Home/News/feed data and rendering; File20 owns only the structural slot host. File25 owns visual truth. File26 owns Search/Discovery/Ranking. File00 remains native identity/security authority even when its reviewed external audit reports release blockers; File20 records that evidence but does not create a replacement membership/security backend.

CF-01 through CF-06 remain declared conditional contract targets, not runtime detection or activation. File20 does not create their domain backends. CF-04 owns token/range/cache truth; current financial law remains one free tier, voluntary donation, zero commission and donor neutrality; CF-06 owns locale/translation truth.

The repository may establish Specified/Coded/Packaged/Automated-QA evidence. Hostinger staging acceptance, Founder acceptance, live deployment and operational acceptance are separate states and are never inferred from this manifest.
