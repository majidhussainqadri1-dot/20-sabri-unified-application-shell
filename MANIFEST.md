# Repository Manifest — File 20

## Historical preserved source

- `SOURCE-ARCHIVE/20-sabri-unified-application-shell-1.0.0-FINAL.zip` is the immutable historical baseline.
- Root `CHECKSUMS.sha256` records that historical archive only.
- `SOURCE-PROVENANCE.md` preserves the import/provenance chain.

## Current source and release flow

- `sabri-unified-application-shell/` is the current reviewed plugin source.
- `.github/workflows/corrective-quality.yml` is the current **1.4.11 eighty-round** release workflow.
- `STATUS.md` is lifecycle truth; current installable artifacts are generated from exact-head CI.

## Production-package constitution

For **1.4.11**, CI derives a clean production source set, excludes tests/development-only files, writes an embedded manifest, builds one canonical plugin root, rejects traversal/duplicate/wrong-root ZIP entries, proves source/stage/extracted file-set + SHA-256 parity, requires embedded/external manifest equality, and verifies archive SHA-256/CRC. The historical File20 HomeFeed producer must not exist in the installable artifact.

The exact-head gate additionally enforces the fresh eighty-round source invariants: File21-only Home/News ownership and five executable native slots; File25-only visual truth; File26-only Search; owner/access/visibility-aware route precedence and duplicate-shortcode collision handling; strict same-origin internal routes; provider semantic-version truth; fail-closed critical File20/File00 health; provider-only doctor/sidebar surfaces; anchored layout/context behavior; schema-5 invariant enforcement; exact recovery ownership and stronger rollback/activation safety; encoded-route hardening; System Check integrity/native-slot evidence; guarded LKG/release-ring/circuit/assurance concurrency; truthful maintenance failure evidence; and exact 18 Future Shell features.

## Recovery/evidence boundary

Recovery snapshots distinguish option absence from `null`, record the stored schema state, and automatic rollback requires compatible snapshot format, File20 code major and current settings schema. Schema upgrades take a pre-upgrade recovery snapshot. Current Emergency safety state and monotonic settings-row concurrency evidence are not silently rolled backward. Restored state is verified and smoke-tested.

## Native-owner and lifecycle boundary

File21 owns Home/News/feed data and rendering; File20 owns only the structural slot host. File25 owns visual truth. File26 owns Search/Discovery/Ranking. File00 remains native identity/security authority; File20 must fail closed rather than invent a replacement identity/security backend.

CF-01 through CF-06 remain declared conditional contract targets, not runtime detection or activation. File20 does not create their domain backends. CF-04 owns token/range/cache truth; current financial law remains one free tier, voluntary donation, zero commission and donor neutrality; CF-06 owns locale/translation truth.

The repository may establish Specified/Coded/Packaged/Automated-QA evidence. Hostinger staging acceptance, Founder acceptance, live deployment and operational acceptance are separate states and are never inferred from this manifest.
