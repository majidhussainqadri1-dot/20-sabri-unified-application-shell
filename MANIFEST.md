# Repository Manifest — File 20

## Historical preserved source

- `SOURCE-ARCHIVE/20-sabri-unified-application-shell-1.0.0-FINAL.zip` is the exact preserved historical baseline.
- Root `CHECKSUMS.sha256` records that historical archive only; it is not the checksum of the current 1.4.x candidate.
- `SOURCE-PROVENANCE.md` documents the original import/provenance chain.

## Current source and release flow

- `sabri-unified-application-shell/` is the current reviewed WordPress plugin source tree.
- `.github/workflows/corrective-quality.yml` is the current 1.4.8 corrective release workflow.
- `STATUS.md` states lifecycle truth.
- `FUTURE-SHELL-V5-18-ENHANCEMENTS.md` and successive ten-round review records preserve approved scope/history.
- Current installable artifacts are generated by exact-head CI and are not treated as source-controlled historical ZIPs.

## Production-package constitution

For the **1.4.8 eighth-audit candidate**, the deterministic workflow:

1. derives a clean production source set from `sabri-unified-application-shell/`;
2. excludes development-only directories including `tests/`, `node_modules`, `.git`, `.github`, coverage/cache folders and temporary/log files;
3. writes embedded `MANIFEST.sha256` from the clean production source set;
4. builds exactly one canonical top-level plugin folder;
5. rejects absolute paths, `..` traversal, duplicate ZIP names and wrong-root entries before extraction;
6. verifies clean source, staged package tree and extracted ZIP have identical file sets and SHA-256 hashes;
7. requires embedded and external source manifests to be identical;
8. verifies deterministic ZIP SHA-256 and CRC integrity;
9. requires the eighth-pass File25 ownership, subdirectory layout/privacy, structured System Check/export, audit-chain retention, repair/rollback, snapshot, Safe Mode/Emergency and admin-controller regressions before packaging;
10. preserves the exact 18 Future Shell features and CF-01..CF-06 conditional/native-owner boundaries.

Development tests remain in the repository and run in CI but do not ship inside the installable WordPress ZIP.

## Recovery/evidence boundary

File 20 1.4.8 snapshots and repairs are limited to File-20-owned state. Automatic rollback requires compatible File 20 code major and current settings schema. Activation snapshot format is integrity protected and does not restore shared WordPress front-page options. System Check export is bounded/sanitized and cannot establish staging/live acceptance.

## Conditional-module evidence boundary

CF-01 through CF-06 remain **declared conditional contract targets, not runtime detection**. They do not activate clinical, support, financial, media-processing, analytics or localization modules. Their installation, health, authorization and activation must be verified independently by native owners and Founder change control where required.

File 20 must not turn CF plan metadata into domain truth. CF-04 owns token-delivery cache/range semantics; CF-03 cannot create paid/premium/donor privilege under the current single-free-tier law; CF-06 owns locale/translation truth.

## Lifecycle evidence boundary

The repository may establish Specified/Coded/Packaged/Automated-QA evidence. Hostinger staging acceptance, live deployment and operational acceptance are separate states and are never inferred from this manifest.
