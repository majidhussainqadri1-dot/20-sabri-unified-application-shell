# File 20 — Fresh Review 1 — 2026-08-17

## Frozen baseline

- Repository baseline reviewed: merged `main` File 20 `1.4.13` at `da5e1c27e20ec490b8ed2189f97af7fc193f2e65`.
- Last proven production state before this review remains File 20 `1.4.12`; repository/package state is not treated as deployed live state.
- Original live incident: `/google-account-security/` HTTP 500 caused by the proven missing Renderer helper block in deployed 1.4.12.

## Review method

The review was completed before corrective edits were started. Runtime helper integrity, release identity, operator migration guidance, staging/live acceptance guidance, changelog continuity, rollback boundaries, Safe Mode operator guidance, exact-head CI behavior and deterministic package identity were re-checked independently.

## Findings

1. **Renderer helper repair — PASS / no new runtime defect found.** Current 1.4.13 contains one definition each of `render_panel()`, `destination_url()`, and `item_visible_to_user()`. Source history confirms these are the immediate pre-deletion helper implementations, not an unrelated reinvention. Permanent unresolved-`self::method()` regression coverage remains present.
2. **DEFECT — migration release truth.** Shipped `MIGRATION.md` still directed operators to File 20 1.4.11 and the obsolete 1.4.11 artifact.
3. **DEFECT — staging/release evidence truth.** Shipped `STAGING-ACCEPTANCE.md` still required recording/installing 1.4.11 rather than the current reviewed release.
4. **DEFECT — changelog continuity.** Shipped `CHANGELOG.md` omitted both 1.4.12 and 1.4.13.
5. **DEFECT — rollback scope truth.** Shipped `ROLLBACK.md` claimed automatic restoration of shared WordPress `show_on_front`, `page_on_front`, and `page_for_posts`, while current `PlanV4Recovery::owned_options()` and activation snapshot code exclude those shared options.
6. **DEFECT — Safe Mode operator truth.** Shipped `ROLLBACK.md` advised raw `?sabri_shell_safe=1`, while current `SafeMode::query_safe_mode()` requires an authenticated administrator plus a valid `_sabri_shell_safe_nonce`.
7. **Release identity decision.** The 1.4.13 production artifact already has an immutable checksum. Correcting shipped operator documents under the same 1.4.13 identity would create two different artifacts with the same runtime version. Review-1 corrections therefore advance to **1.4.14**.

## Corrective scope

- Preserve the 1.4.13 Renderer runtime repair unchanged.
- Advance release/runtime/package identity to 1.4.14.
- Correct current migration, staging/live acceptance, changelog and rollback/Safe Mode operator truth.
- Add permanent `run-release-documentation-truth.php` regression coverage.
- Preserve exact-head CI checkout and all existing File20 ownership, Future Shell, routing, recovery, privacy and integration gates.

## Evidence boundary

Repository correction, CI, package integrity and merge status do not establish production resolution. Final incident closure still requires exact 1.4.14 deployment, `/google-account-security/` live re-test and deployed parity confirmation.
